@inject appEvaluaciones.Shared.Services.IAuthService Auth\n@inject NavigationManager Nav\n\n<div class="top-row ps-3 navbar navbar-dark">\n    <div class="container-fluid d-flex justify-content-between align-items-center">\n        <a class="navbar-brand" href="">appEvaluaciones</a>\n        <div class="text-end">\n            @if (Auth.Current.IsAuthenticated)\n            {\n                <span class="me-2 small">@($"Usuario: {Auth.Current.Username} ({Auth.Current.Role})")</span>\n                <button class="btn btn-sm btn-outline-light" @onclick="OnLogout">Salir</button>\n            }\n            else\n            {\n                <NavLink class="btn btn-sm btn-outline-light" href="login">Login</NavLink>\n            }\n        </div>\n    </div>\n    </div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-item px-3 mt-2">
            <NavLink class="nav-link" href="evaluacion">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> EvaluaciÃ³n
            </NavLink>
        </div>

        <!-- MenÃº CatÃ¡logos con sub-items -->
        <div class="nav-item px-3 mt-3">
            <button class="nav-link btn btn-link text-decoration-none px-0" @onclick:stopPropagation="true" @onclick="ToggleCatalogos" aria-expanded="@catalogosOpen" type="button">
                <span aria-hidden="true">@(catalogosOpen ? "▼" : "►")</span> CatÃ¡logos
            </button>
        </div>
        @if (catalogosOpen && Auth.Current.IsAuthenticated && string.Equals(Auth.Current.Role, "Admin", StringComparison.OrdinalIgnoreCase))
        {
            <div class="nav-item px-4">
                <NavLink class="nav-link" href="tiendas">
                    <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Tiendas
                </NavLink>
            </div>
            <div class="nav-item px-4">
                <NavLink class="nav-link" href="empresas">
                    <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Empresas
                </NavLink>
            </div>
            <div class="nav-item px-4">
                <NavLink class="nav-link" href="categorias">
                    <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> CategorÃ­as
                </NavLink>
            </div>
            <div class="nav-item px-4">
                <NavLink class="nav-link" href="preguntas">
                    <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Preguntas
                </NavLink>
            </div>
            <div class="nav-item px-4">
                <NavLink class="nav-link" href="gerentes">
                    <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Gerentes
                </NavLink>
            </div>
            <div class="nav-item px-4">
                <NavLink class="nav-link" href="evaluadores">
                    <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Evaluadores
                </NavLink>
            </div>
        }
        <div class="nav-item px-3 mt-3">
            <NavLink class="nav-link" href="login">
                <span class="bi bi-person" aria-hidden="true"></span> Login
            </NavLink>
        </div>
    </nav>
</div>

@code {\n    private bool catalogosOpen = false;\n    private void ToggleCatalogos() => catalogosOpen = !catalogosOpen;\n\n    private async Task OnLogout()\n    {\n        await Auth.LogoutAsync();\n        Nav.NavigateTo("/login", forceLoad: true);\n    }\n}

